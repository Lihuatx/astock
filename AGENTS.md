# astock 项目规范

## 项目目标

构建面向个人研究的 A 股模拟交易系统。P0～P3 已完成，当前追加 P4：观测事实、复盘快照、Windows runner、只读 Dashboard、私网同步、备份与驻留化。

## 长期治理与事实归属

- 本项目的代码、架构、测试、技术进度和验收结论以本仓库为唯一事实源。
- `D:\codex_work\yancey-os` 只管理个人组合优先级、时间预算、内容节奏、职业安全和商业验证；跨仓库同步只记录来源 commit、数据截止时间和对总目标的影响，不复制本项目技术待办。
- 首个治理周期为 2026-07-13 至 2026-10-10；完整 P4 目标日期为 2026-08-09。截止日期不得覆盖安全门、独立审计或真实恢复验收。
- `astock` 是当前唯一公开主项目；对外内容只能引用稳定 commit 和已确认的证据包。
- 每周六自动周审严格只读，只能提出本文件和 `ROADMAP.md` 的建议补丁；未经 Yancey 确认不得自动落盘、提交或发布。

## Agent 职责分离与证据

- 实施 Agent：按已确认范围修改代码和规范，完成验证后同步更新 `ROADMAP.md`。
- 审计 Agent：必须从稳定 commit 和新上下文开始，只读复核实施结果，不得用实施 Agent 的自述代替证据，也不得在同一上下文直接修复后自审。
- 编辑 Agent：只能消费已确认的 `ReviewBundle`、测试和审计证据，不得重新计算收益、回撤、成交数或测试数。
- Yancey：决定范围、风险预算、策略与参数解冻、红灯恢复、外部系统变更和公开发布。
- “已完成”必须同时具备实现、验证结果、验证日期和稳定 commit；未验证或仅存在代码不得标记完成。
- 严重正确性问题必须形成单一证据包，至少包含文件位置、时序说明、最小复现、修复前失败测试、修复后回归测试、结果差异和残余风险。修复未实施时，修复后证据必须明确标为“待实施”，不得伪造闭环。

## 固定边界

- Windows 本机运行，通达信 TdxQuant 为主行情源。
- 同花顺 Financial-API 仅做历史补充和交叉校验，不在盘中自动接管主行情。
- 初始虚拟资金 100000 元，现金账户，只做多；目标是扣除完整交易成本后长期年化收益争取达到 15%，最大回撤目标不超过 25%。
- 只做中低频，不做日内高频；候选策略每 10～40 个交易日调仓，严禁为了命中收益目标过度拟合参数。
- 允许建立与稳健基线隔离的激进研究账户：按 30000 元实验资金、3～5 只持仓、2～10 个交易日调仓、单票不超过 25% 回测；禁止融资、杠杆和期权。
- 激进研究以年化 25%～40% 和最大回撤不超过 25% 作为观察区间；月收益 10% 仅统计命中月份，禁止作为参数优化目标。
- 激进研究必须同时报告 2021 年以来的分段结果和最近 18 个月诊断；不得用缩短样本替代长历史鲁棒性检验。
- 只支持沪深主板普通 A 股、连续竞价、限价单；研究和模拟盘支持策略独立账户及组合账户。
- 策略输出 `TradeIntent`，不得直接操作 Broker。
- 策略只能使用信号时点已经发布的数据；日线信号最早在下一交易日成交，禁止未来函数、全样本归一化和样本外参数回看。
- V2 时间隔离固定为 2021～2023 参数研究、2024／2025 双验证、2026 压力测试；真正未观察样本只来自后续前向模拟盘。
- 因子网格必须检查最低成交数和相邻参数稳定性，最终三个候选优先来自不同逻辑家族。
- V3 候选只能依据 2024／2025 双验证收益与风险选择；2026 压力测试禁止参与候选替换。
- 策略多样性使用双验证日收益相关性软约束，不得仅凭人工家族标签硬去重。
- 候选必须报告参数平坦度、年度成交数、持仓覆盖率和 10／20／30BP 滑点敏感性。
- 历史研究基准使用单边 10BP，工作日前向模拟采用更保守的单边 20BP 执行滑点。
- Tushare 第三方代理仅用于历史基本面与估值，TDX 仍是行情主源；代理请求频率上限为每分钟 60 次并必须本地缓存。
- 基本面记录在 `ann_date` 后的下一交易日才可进入信号；同报告期只使用信号时点已发布的最新版本。
- 行业研究固定使用申万 2021 版一级行业；股票行业归属必须按 `in_date/out_date` 逐日重建，禁止用当前行业成分回填历史。
- 行业中性因子只能在信号日所属行业内部标准化；行业轮动只能使用信号日及此前的行业行情和成分股市场宽度。
- 2026 仍只作压力测试，禁止依据其表现选择基本面因子或参数。
- 风控位于策略与 OMS 之间，Broker 保留最终交易规则校验。
- 模拟盘使用自建 `AShareSimBroker`，不得用通达信模拟成交结果替代内部账本。
- 当前禁止连接真实证券账户，禁止调用通达信下单、撤单和账户交易函数。

## 安全与配置

- `.env*`、数据库、日志、备份、原始行情和报告不得提交 Git。
- `.env.demo` 是本机真实配置文件，代码可在运行时加载，开发和汇报不得读取或回显其值。
- 所有凭据只通过环境变量进入进程。
- Dashboard 只允许展示和复盘，不得提供下单、撤单、修改资金、切换策略或触发模拟执行的入口。
- Dashboard 通过 Tailscale 私网访问，首版不开放公网端口、不配置公网域名、不依赖 Cloudflare。
- 服务器目标只监听 `127.0.0.1:18080`，由 Tailscale Serve 提供私网 HTTPS；禁止 Funnel 和任何公网端口。
- Windows 本机账户 SQLite／Ledger 是交易权威事实源；服务器只保存不可变 `ReviewBundle`、`LiveStatus` 和查询索引。
- 同步失败不得阻断本地模拟交易；行情过期、订单未知或账实不符仍必须 fail closed。
- 自动模拟执行默认关闭，完成 5 分钟成交复核和首次调仓验收后才允许单独启用。
- GitHub Actions 只允许运行 Python 测试、前端检查／构建和 Docker build；禁止推送镜像、部署、读取生产密钥或触发外部发布。
- Docker、Tailscale、Windows 计划任务、CI/CD、数据库 schema／迁移和服务器部署在实际修改或执行前必须逐项说明风险并获得 Yancey 确认。
- 不自动发布文章、视频、站点或制品；任何公开发布必须单独确认。

## 工程约定

- Python 3.13，源代码位于 `src/astock`，测试位于 `tests`。
- P4 目录固定为：`src/astock/observability` 保存审计、快照、Bundle 和同步；`src/astock/dashboard` 保存 FastAPI 与服务端查询；`web` 保存 React／TypeScript／Vite；`deploy` 保存 Docker Compose、部署和备份脚本。
- 前端使用 Node.js 22；生产环境由 FastAPI 同源提供 API 和已构建静态资源。
- 金额、价格、费用统一使用 `Decimal`；时间统一使用带 `Asia/Shanghai` 时区的 `datetime`。
- SQLite 使用 WAL、外键和单写者；订单写入 Outbox 后才允许交给 Broker。
- `ReviewBundle` 是字节级确定的不可变发布快照；生成时间必须来自已持久化事实并参与完整性校验，同一事实重复生成必须得到相同字节。
- 每版 `LiveStatus` 必须拥有独立稳定 ID 和不可变本地 payload；同步 Outbox 不得用新心跳覆盖尚未发送的旧版本。
- 跨策略集合、账户和代次的事件 ID 必须包含账户作用域；同一 ID 的不同内容必须报错，禁止用 `INSERT OR IGNORE` 静默吞掉冲突。
- 除 `/healthz` 和带独立 Bearer token 的上传接口外，SPA、静态资源和浏览器查询 API 必须统一验证 Tailscale 身份允许列表。
- FastAPI 共享资源必须由 lifespan 创建和关闭；SQLite 并发访问使用每操作独立连接或明确的串行锁。
- 在线备份必须同时包含 SQLite、不可变 Bundle 和逐文件 SHA256 manifest，并通过独立目录真实恢复验证。
- 浏览器缓存只能保存主题等非权威偏好，不得保存账户、信号、订单或健康状态的权威副本。
- 外部 API 必须有超时、有限重试、原始响应落地和明确错误分类。
- 行情过期、订单未知、账实不符时必须 fail closed，停止创建新订单。
- 修改后运行与改动风险相称的测试，并同步更新 `ROADMAP.md`。

## Git

- 默认分支 `main`。
- 本地提交可直接执行；push 前必须获得用户单独确认。
- Commit 使用中文描述及规定前缀。
