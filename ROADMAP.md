# ROADMAP

## 当前阶段

P0～P3、V3～V8 已按修复后的开盘数据口径重跑。P4 第 1 周原审计 P0／P1 问题已在稳定 commit `73bc83e` 完成第三次独立复审并全部转绿；完整 P4 目标日期为 2026-08-09。TDX 模拟账户权威、SQLite 镜像与每日后验复盘模式已实现并完成 v2 schema 迁移；新量化模拟客户端的本地 TQ SDK 只读账户接口已联调通过，现有 HTTP 传输层尚需适配后才能发送模拟订单，自动执行继续关闭。

## 已完成

- 已冻结第一期范围：Windows 本机、TDX 主行情、THS 交叉校验、TDX 模拟账户执行、本地审计镜像、禁止真实交易。
- 稳健账户目标已调整为长期年化收益争取达到 15%、最大回撤不超过 25%；激进研究原有 25% 回撤观察线不变。
- 已验证 TQ 本地 HTTP、Python 初始化、快照、订阅、取消订阅和全市场代码表。
- 已建立项目级 `AGENTS.md`。
- P0：已完成仓库治理、架构、安全边界、安装元数据、CLI 和验收规范。
- P1：已完成 TDX／THS 适配、原始事件落地、标准化和行情质量检查。
- P2：已完成 A 股模拟 Broker、T＋1、整手、费用、冻结、部分成交、撤单和重启恢复。
- P3：已完成动量趋势策略、独立风控、持久化 OMS／Outbox、账本、历史回放和逐日对账。
- 29 项自动测试全部通过。
- 真实 doctor 已通过：TDX、THS、代码表、日线和交易日历均可用。
- 真实历史回放已通过：367 根日线、10 笔成交、全部交易日对账成功。
- 已加载 3194 只沪深主板、1196 个交易日的前复权日线，并校验 TDX 单批最多 10 只和成交额万元单位。
- 第一轮六因子报告已归档，2026 因已被观察而降级为压力测试，不再称为完全样本外。
- V2 旧结果因开盘仓位计算使用 T＋1 收盘价、成交判断使用 T＋1 日内高低价而失效，已由修复后重跑的 V3～V8 取代，禁止继续引用其候选和指标。
- V3 取消家族硬去重，仅使用 2024／2025 双验证分数和日收益相关性软惩罚；修复后观察项为低波动趋势、布林回归、RSI 超卖反转，三者均未达到双验证目标。
- V3 已完成参数平坦度、持仓覆盖率、成交置信度、10／20／30BP 滑点和缩量阈值敏感性报告。
- 工作日前向模拟执行采用单边 20BP 保守滑点，研究基准仍为单边 10BP。
- V3 三个观察项均为标准置信度；2024／2025 年化分别为低波动趋势 2.79%／5.18%、布林回归 -9.25%／20.62%、RSI 超卖反转 -7.82%／7.69%。
- Tushare 第三方代理六类接口已验证，64 个财务批次和 118 个信号日估值已本地缓存；代理 token 不能直连官方接口。
- V4／V7 已按 26 个量价与点时基本面因子、702 组配置重跑，跨日因果与开盘字段可用性审计违规数为 0。
- 修复后基本面观察项为现金流转化、现金质量价值和基本面价值，三者均未达到双验证目标。
- 基本面价值研究期年化 6.61%，2024 年化 12.58%，2025 年化 0.20%，因每年仅 15～17 笔成交被标记为低置信度。
- 已建立独立策略账户和组合观察账户；现有账户订单数、成交数均为 0，没有受旧调仓逻辑污染。
- 已修复模拟盘未执行 `rebalance_days` 的问题：每个策略独立持久化最近信号／执行日期，非调仓日跳过，同一计划重复执行保持幂等。
- 已移除模拟执行对 `MomentumTrendStrategy` 的错误依赖，改为按各因子目标股票生成带策略原因的中性调仓意图。
- 已修复回测 T＋1 开盘前读取当日收盘价和高低价的问题；开盘仓位只使用前收盘与当日开盘，成交审计记录数量、价格和可用字段。
- V5 已按 30000 元隔离资金、3～5 只持仓、2～10 日调仓和单边 20BP 滑点完成 20 因子、360 组激进参数回测。
- V5 没有策略同时通过长历史稳定门、最近 18 个月有效交易和激进收益观察线，因此没有新增激进模拟盘候选。
- `fundamental_quality_growth` 最近 18 个月年化 27.35%、最大回撤 23.55%，但修复后研究期年化 -11.33% 且正邻域占比为 0%，已按规则拒绝。
- V6 已接入申万 2021 一级行业的 `in_date/out_date` 点时成分，31 个行业、当日有效股票行业归属覆盖率 91.66%。
- V6 修复后行业内基本面价值与现金质量价值具有稳定且正验证增量，但均未达到双验证目标；基本面价值 2024／2025 年化为 -4.99%／30.74%，现金质量价值为 6.78%／23.44%。
- 行业轮动冻结为 2 个行业×2 只股票、40 日调仓；研究期／2024／2025／2026 压力年化分别为 3.52%／17.03%／0.06%／34.24%，且仅 33% 研究配置得分为正，暂不进入模拟盘。
- V7 已扩展经营现金流转化、应收账款周转、存货周转和营业外损益占比四个点时字段，全部沿用公告日后下一交易日生效和历史修订覆盖规则。
- V7 已完成 26 个因子、702 组配置回测；现金流转化和现金质量价值组合通过研究稳定门，但均未达到 2024／2025 双验证目标，因此不替换现有前向模拟候选。
- `cash_conversion` 研究期／2024／2025／2026 压力年化分别为 5.68%／0.40%／41.62%／-6.63%；跨年差异显著，只保留研究观察。
- `cash_quality_value` 研究期／2024／2025／2026 压力年化分别为 4.58%／10.73%／7.83%／-10.49%，参数邻域为 100% 正，但收益未达到目标。
- 应收、存货及组合营运效率因子相邻参数稳定性不足，营业外损益独立性研究期为负，均已拒绝。
- V8 已完成 CGO 独立做多因子研究；126 日／5 只／40 日单点最优在研究期年化 12.19%、最大回撤 30.65%，2024／2025 年化 -17.31%／53.16%，相邻参数正得分占比 0%，已拒绝。
- P4 原 P0-1、P0-2、P1-1～P1-5 审计问题已在 commit `73bc83e` 完成实现、51 项 Python 测试和第三次全新上下文独立复审，7 项全部为绿；证据见 `docs/P4_REAUDIT_2026-07-13.md`。该结论只关闭原审计红灯，不代表 P4 整体完成。
- 100 天 5 分钟成交复核已用本机 TdxQuant 61,104 根和 `pytdx` 历史补充 30,096 根完成，19 只股票共 91,200 根、100 个交易日覆盖率 100％；结果见 `docs/INTRADAY_EXECUTION_REVIEW_RESULT.md`。完成复核不等于通过安全门。
- 2026-07-14 已用 2026-07-13 完整收盘数据生成并人工执行当前策略集计划；三个策略均因市场宽度不足保持空仓，三个账户 0 订单、0 成交且账实一致，重复执行均按 `signal_already_executed` 跳过。该结果只完成零订单决策链，不等于首次真实订单路径验收。
- TDX 模拟交易适配器已按官方 `stock_account`、`order_stock`、`cancel_order_stock`、资产、持仓和委托查询协议实现；普通账户的 `Value=1` 待确认响应会拒绝，只有 `Value=2` 且带 `Wtbh` 才记为模拟订单 ACK。
- 观测库已从 schema v1 迁移至 v2，新增不可重发的 TDX 模拟订单 Outbox 和不可变每日事实镜像；迁移前备份完整性、迁移后旧事件／账户计数和 SQLite `integrity_check` 均通过。
- runner 已改为盘中向单一 TDX 模拟组合账户发送三个策略标的并集，单笔错误写结构化日志后继续；15:20 后统一镜像资产、持仓和委托并生成复盘差异与组合账户快照。

## 进行中

- 复核数据已完整但三个策略仍未达到原成交质量阈值；该结果继续进入每日复盘，不再阻止 TDX 模拟账户前向执行，也不得通过改阈值或删不利订单美化报告。
- 将前向交易适配器从当前不可用的 `127.0.0.1:17709` HTTP 传输切换为新量化模拟客户端自带的本地 `tqcenter.py` SDK；随后验证模拟下单返回、重启幂等和收盘复盘，再单独配置自动执行开关。

## P4 首月四周验收（2026-07-13 至 2026-08-09）

### 第 1 周：稳定交接、独立审计与事实源

- [x] 形成稳定交接 commit，并以新上下文开展独立只读审计。
- [x] 运行当前 41 项 Python 测试和临时目录最小复现；测试全绿但审计发现关键盲区。
- [x] 修复并验证确定性 `ReviewBundle`、多版本 `LiveStatus` Outbox、跨账户 ID、静态资源鉴权、FastAPI／SQLite 生命周期和完整备份恢复。
- [x] 严重问题逐项补齐修复前失败测试、修复后回归结果和残余风险。

### 第 2 周：runner、补传与只读后端

- [ ] 验收 Windows runner 单实例与重启恢复、持久化 Outbox、断网后按版本幂等补传和 FastAPI 上传／查询接口。
- [ ] 自动 `paper-execute` 保持默认关闭直至模拟账户类型、交易协议和重启幂等验证完成；服务器故障及盘中行情／单笔订单／对账错误只记录日志，不阻断其他模拟订单。

### 第 3 周：Dashboard、离线制品与可恢复备份

- [ ] 验收 React／TypeScript／Vite Dashboard、360px Android 浏览器布局和同一 `ReviewBundle` 生成的离线 HTML。
- [ ] 验收 Docker build、SQLite＋Bundle＋SHA256 manifest 备份与独立目录恢复。
- [ ] GitHub Actions 只能运行 Python 测试、前端检查／构建和 `push: false` 的 Docker build，不得发布或部署。

### 第 4 周：隔离私网部署与真实演练

- [ ] 获得逐项确认后，在现有服务器以独立目录、容器、数据卷和 `127.0.0.1:18080` 部署。
- [ ] 配置 Tailscale Serve；禁止 Funnel、公网端口和公网域名。
- [ ] 验收 Android 浏览器私网访问、断网补传、60 秒数据延迟、90 秒断联告警、Windows runner／服务器容器重启和真实恢复演练。

### 首月并行硬结果

- [ ] 至少 5 个交易日保存完整运行回执，并能区分正常无信号、任务未运行和运行失败。
- [x] 完成 100 天 5 分钟成交复核，记录可成交率、滑点、不可成交原因和证据缺口；三个策略均未通过，该结论保留为后验质量诊断。

## 待办

- P4 上线验收：Tailscale Android 浏览器访问、站内告警、完整在线备份和真实恢复演练。
- 工作日盘中实时延迟、断线恢复和多策略模拟成交验收。

## 阻塞

- 盘中实时延迟、断线和回调覆盖率需在交易时段验收。
- 当前没有达到双验证目标的策略，前向账户仅用于执行链观察，不得表述为合格候选或可实盘策略。
- 本机未安装 Docker，镜像和 Compose 只能由 GitHub CI 或 Ubuntu 服务器验证；安装服务器软件和 Git push 等待单独确认。
- Tailscale 在腾讯云和 Android 之间的实际连通性尚未验证。
- 新量化模拟客户端未监听官方文档所述的 `127.0.0.1:17709` HTTP 端口，但 `D:\new_tdx_mock\PYPlugins\user\tqcenter.py` 直连初始化、当前登录账户句柄、资产、持仓和当日委托查询均已成功；现有 HTTP 适配器不能据此发送订单，需先实现并测试本地 SDK 传输层。
- 审计后的 commit `7e3c7f9`、`8757a2d` 已修改 CI 与 Docker／Compose 配置；本轮未继续触碰这些红线文件。运行 workflow、Docker、部署或外部发布仍需 Yancey 单独确认，并须纳入独立复审。

## 最近验证

- 2026-07-11：TDX 快照连续 30 次成功，HTTP 本地平均延迟 8.39ms、P95 10.73ms；该指标不代表交易所行情延迟。
- 2026-07-11：TDX doctor 返回 5534 个证券代码、最新日线 2026-07-10、29 个近期交易日；THS 快照及源时间正常。
- 2026-07-11：`000001.SZ` 从 2025-01-02 至 2026-07-10 回放 367 根日线，生成 10 笔成交，逐日对账全部通过。
- 2026-07-11：`python -m unittest discover -s tests -v` 共 12 项全部通过。
- 2026-07-11：16 项自动测试全部通过；未来数据扰动不改变历史信号，全部成交均晚于信号日。
- 2026-07-11：三候选研究结果已记录于 `docs/STRATEGY_RESEARCH.md`，没有策略达到验证期目标，严禁包装为可实盘策略。
- 2026-07-11：V2 因子扩展和 405 组参数回测完成，完整结果见 `docs/STRATEGY_RESEARCH_V2.md`；未来函数审计违规数为 0。
- 2026-07-11：V3 相关性软约束选出低波动趋势、短期反转、上涨／下跌波动率比，验证期相关性为 0.27～0.43，完整结果见 `docs/STRATEGY_RESEARCH_V3.md`。
- 2026-07-11：V4 点时基本面联合研究完成，完整 20 因子结果、覆盖率和代理风险见 `docs/STRATEGY_RESEARCH_V4.md`。
- 2026-07-11：V5 激进研究完成，20 项自动测试通过，成交因果违规数为 0；完整结论见 `docs/STRATEGY_RESEARCH_V5_AGGRESSIVE.md`。
- 2026-07-11：V6 申万一级行业研究完成，22 项自动测试通过，所有行业策略成交因果违规数为 0；完整结论见 `docs/STRATEGY_RESEARCH_V6_INDUSTRY.md`。
- 2026-07-12：V7 现金流质量与营运效率研究完成，23 项自动测试通过，全部成交因果违规数为 0；完整结论见 `docs/STRATEGY_RESEARCH_V7.md`。
- 2026-07-12：修复 T＋1 开盘仓位使用当日收盘价及成交判断使用当日高低价的日内未来数据问题；新增开盘字段扰动回归测试和成交数量／价格／字段审计。
- 2026-07-12：模拟盘已按策略独立维护调仓周期，并验证调仓状态落盘、非调仓日跳过和同一计划幂等；29 项自动测试全部通过。
- 2026-07-12：V3～V8 已按修复口径重跑，当前无策略达到 2024／2025 双验证目标；CGO 明确拒绝，完整结论见 `docs/STRATEGY_RESEARCH_V8_CGO.md`。
- 2026-07-12：稳健账户最大回撤目标由 15% 调整为 25%，相关主研究、行业研究和 CGO 已重跑；达标策略数量仍为 0，候选排序未变化。
- 2026-07-12：独立重跑当前 41 项 Python 自动测试全部通过；第一次遗漏 `PYTHONPATH=src` 的收集失败已单独记录为命令环境错误。41 项测试未覆盖本次审计发现的 P0／P1 问题，不能据此标记 P4 完成。
- 2026-07-12：以最新报告建立 `set-da7342dcdd2fd5ab` 观察集，4 个新账户均为 100000 元、0 持仓、0 订单、0 成交；11 个原账户登记为 legacy，未移动或删除。
- 2026-07-12：稳定交接版本已生成 ReviewBundle 与 1.08MB 离线单文件报告；独立复现证明当前 Bundle 不是字节级确定，相关“不可变”验收撤回，待修复后重新验证。Docker 仍因本机未安装而未验证。
- 2026-07-12：最小复现得到“同 ID／同哈希／不同 Bundle 字节、两版心跳只剩 1 行、两账户同 ID 事件只剩 1 行、未认证静态资源返回 200”；完整证据见 `docs/P4_AUDIT_2026-07-12.md`。
- 2026-07-13：分钟成交实现提交后运行 `python -m unittest discover -s tests -v`，48 项 Python 自动测试全部通过；包含分钟成交限价窗口、跨账户流动性不重复使用、缺失证据不得误判通过和 TDX 分钟字段单位回归。该结果不是独立复审结论。
- 2026-07-13：冻结 100 日 5 分钟复核口径：开盘后 30 分钟主窗口、5／15／30 分钟分项、单根 K 线 10％参与率、99％数据覆盖、95％金额成交率、90％完整订单率和买卖 P95 不利滑点不超过 20BP。
- 2026-07-13：基于 commit `efae056` 运行首轮 100 日口径诊断，计划订单 25 笔；TDX 实际分钟覆盖率仅 67％。`cash_conversion` 的 15 笔均因 2～3 月数据缺失未成交；`cash_quality_value` 10 笔中 9 笔完整成交、1 笔限价未触及，金额成交率 89.73％；`fundamental_value` 无计划订单，三者均未通过。结果见 `docs/INTRADAY_EXECUTION_REVIEW_RESULT.md`。
- 2026-07-13：对稳定 commit `41f8228` 开展全新上下文独立只读复审；P0-1、P0-2、P1-3、P1-4 为绿，P1-1、P1-2、P1-5 仍红。复现证明服务器 Bundle 文件被篡改后重传未冲突、旧状态迟到会覆盖最新视图、备份缺少版本信息制品；另发现匿名 `/openapi.json` 返回 200 的残余暴露。
- 2026-07-13：已为上述残余问题增加正式回归：服务器逐字节核对既有 Bundle、LiveStatus 按 `generated_at` 防止乱序回退并保留各版 receipt、备份加入 `version.json` 且纳入 SHA256 manifest、禁用匿名 OpenAPI。Python 50 项全部通过；前端首次误在仓库根目录运行因缺少 `package.json` 失败，切换至 `web` 后 1 项 Vitest 通过。独立复审待新稳定 commit。
- 2026-07-13：对稳定 commit `e6cba45` 开展第二次全新上下文独立只读复审；P0-1、P0-2、P1-1～P1-4 为绿，P1-5 仍红。复现证明从 manifest 中删除 `server.db` 条目但保留文件后校验仍通过；Python 50 项、Vitest 1 项、TypeScript、Dashboard build 和离线报告 build 均通过，Docker 与 `sh` 因本机未安装未验证。
- 2026-07-13：已补备份 manifest 双向文件集合校验，任何漏声明或额外制品均失败；同一 `generated_at` 的不同 LiveStatus 按 `status_id` 稳定决胜，消除到达顺序歧义。Python 51 项全部通过，独立复审待新稳定 commit。
- 2026-07-13：对稳定 commit `73bc83e` 开展第三次全新上下文独立只读复审；P0-1、P0-2、P1-1～P1-5 共 7 项全部为绿，原审计红灯关闭。独立复现覆盖 manifest 漏声明、额外未声明文件、损坏／缺失制品、空目录恢复、同时间状态乱序、跨账户冲突和并发备份；Python 51 项、Vitest 1 项通过。P4 整体仍受分钟数据、首次调仓和真实部署验收约束。
- 2026-07-13 17:26：开盘执行自动化完成 fail closed 验收。TDX doctor 通过，交易日历和最新日线均为 2026-07-13，快照买一／卖一为 10.54／10.55 且接收日期为当日；但已处于连续竞价时段外，未运行 `paper-execute`、未创建订单。另确认 `data/paper/pending_signals.json` 的 signal_date 为 2026-07-10，但该旧计划未绑定当前策略集合 `set-da7342dcdd2fd5ab`，当前集合四个策略账户存在且无当前待执行计划；即使在时段内也必须先重新生成当前集合计划。旧账户订单／成交均为 0。
- 2026-07-14：稳定 commit `b7ee3f3` 增加 `pytdx` 只读历史分页补数，本机 TdxQuant 仍优先且重叠时间戳不会被覆盖；19 只股票共 91,200 根 5 分钟线覆盖 2026-02-06 至 2026-07-10 的 100 个交易日，覆盖率 100％。订单执行窗口内 60 根双源重叠 K 线价格和成交量完全一致；全量 Python 53 项通过。
- 2026-07-14：100 日复核最终结果仍不通过。`cash_conversion` 15 单中 14 单完整成交、1 单限价未触及，金额成交率 93.58％；`cash_quality_value` 10 单中 9 单完整成交、1 单限价未触及，金额成交率 89.73％；`fundamental_value` 无计划订单且买卖方向证据不足。自动模拟执行保持关闭。
- 2026-07-14：市场面板和点时基本面刷新至 2026-07-13，当前 `set-da7342dcdd2fd5ab` 计划执行日为 2026-07-14；三策略均因市场宽度不足选择 0 只。09:33 使用临时进程开关人工执行，TdxQuant 实时快照连续 17 秒冻结且分钟线仅到 2026-07-10，先后两次 fail closed；随后由 `pytdx` 当日 09:35 五分钟桶确认开市，三账户均 0 订单、0 成交、账实一致，重复执行均跳过，持久配置仍为关闭。全量 Python 55 项通过；首次真实订单路径仍未验收。
- 2026-07-14：按 TDX 模拟账户权威与每日后验复盘的新规范完成适配器、Outbox、事实镜像、组合执行器和 runner 改造；迁移前备份为 `data/backups/observability-pre-v2-20260714-101205.db`，v2 迁移后保留 7 条审计事件和 15 个账户登记，新增表为空且 `integrity_check=ok`。未配置账号故障演练只写日志、0 Outbox、0 事实版本；Outbox 只领取当前策略集与信号日订单，限价按最小价位向交易方向取整；全量 Python 65 项通过。
- 2026-07-14：确认当前运行客户端为 `D:\new_tdx_mock\TdxW.exe`。官方 HTTP 固定端口 `127.0.0.1:17709` 未监听，客户端自身 `14572` 端口不是 HTTP；按新客户端自带官方示例通过 `tqcenter.py` 初始化后，当前登录股票账户句柄有效，资产／持仓／当日委托查询均成功，模拟资产 1000000 元、持仓 0、当日委托 0。该结果只证明本地 SDK 只读链路，不代表下单、撤单、重启幂等或收盘复盘已验收。
