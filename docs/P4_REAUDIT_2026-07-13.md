# P4 独立复审证据包（2026-07-13）

## 范围与结论

- 原始红灯证据：`docs/P4_AUDIT_2026-07-12.md`。
- 最终审计 commit：`73bc83e382cf375eabce8c0a12b12be8bf5cdf6a`。
- 审计方式：从稳定 commit 和全新上下文开始，只读使用操作系统临时目录复现；未修改仓库、未写正式 `data`、未提交。
- 结论：原 P0-1、P0-2、P1-1～P1-5 共 7 项全部为绿，2026-07-12 原审计红灯可以关闭。
- 边界：本结论不代表 P4 整体完成；Docker、真实服务器恢复、Tailscale／Android、Windows runner 重启、断网补传、100 日分钟成交复核和首次真实订单路径仍待验收。

## 修复与独立证据

| 审计项 | 结论 | 修复前失败 | 修复后独立复现 |
| --- | --- | --- | --- |
| P0-1 CI／端口 | 绿 | workflow 具有镜像发布能力，Compose 偏离 18080 | 无发布权限、registry login 或 `push: true`；Docker build 为 `push: false`；主机仅映射 `127.0.0.1:18080` |
| P0-2 身份校验 | 绿 | 未认证 `/assets/app.js` 返回 200 | 匿名 SPA、静态资源、查询 API 均为 403；允许身份为 200；OpenAPI 禁用 |
| P1-1 Bundle 确定性 | 绿 | 同 ID／同哈希可对应不同字节；服务器不核对既有实际文件 | 同输入 ID、哈希、字节完全相同；修改 `generated_at` 后旧完整性失败；既有文件篡改或缺失后同 ID 重传均返回冲突 |
| P1-2 LiveStatus | 绿 | 新心跳覆盖未发送旧版；旧状态迟到使服务器最新视图回退 | 本地 Outbox 保留两个不可变版本；旧状态迟到不回退状态或告警；同 `generated_at` 按 `status_id` 稳定决胜，两种到达顺序结果一致 |
| P1-3 跨账户 ID | 绿 | 两账户相同局部 ID 的第二条事件被静默吞掉 | 两账户生成不同账户作用域 ID并保存两行；同作用域 ID 不同内容显式拒绝 |
| P1-4 生命周期／并发 | 绿 | lifespan 外创建 Store，共享 SQLite 连接无锁 | Store 仅在 lifespan 创建和关闭；`RLock` 串行保护；24 次并发写入、查询和在线备份无异常，SQLite 完整性均为 `ok` |
| P1-5 完整备份恢复 | 绿 | 缺少逐文件 manifest、版本信息和真实文件恢复；曾存在 manifest 只单向校验 | 有效包包含 `server.db`、`version.json`、全部 Bundle 和 manifest；漏声明、额外未声明、损坏或缺失任一关键文件均失败；空目录恢复后 Bundle、状态、告警、overview 和复盘 API 可查询 |

## 最终验证

- `python -m unittest discover -s tests -v`：51 项通过，退出码 0。
- `npm test`：1 个 Vitest 文件、1 项测试通过，退出码 0。
- 审计前后 `git status --short` 均无输出，HEAD 保持 `73bc83e382cf375eabce8c0a12b12be8bf5cdf6a`。
- 第二次复审还验证 TypeScript、Dashboard build 和离线报告 build 通过；Docker 与 `sh` 因本机未安装未验证。

## 残余风险

- 当前并发证据只覆盖单进程锁，不覆盖多进程共同写同一 SQLite。
- SHA256 manifest 可发现意外损坏，但没有签名，不能抵御恶意同时篡改文件和 manifest。
- Bundle 文件与 SQLite 索引仍是双存储，异常中断后的孤儿文件恢复需在真实演练中验证。
- Docker build、真实服务器恢复、Tailscale／Android、runner 重启和断网补传不属于本次原红灯关闭证据。

